name: Test-method Suite
tests:
-  name: SauceLabs EN EDGE
   parameters:
      browser: EDGE
#The following parameters are optional, the defaults are taken from Utils
      language: en
      country: US
      architecture: X64
      version: latest
   classes:
   - com.watermelon.steps.CucumberEdgeRunner